{
	"name": "poker-deck",
	"version": "1.0.0",
	"description": "This is the poker deck class demo",
	"main": "poker-deck.js",
	"scripts": {
		"test": "mocha --recursive \"./test/**/*.js\" --exit --slow 5000 --async-only"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/tclzcja/poker-class-demo"
	},
	"license": "MIT",
	"devDependencies": {
		"mocha": "^7.1.1"
	},
	"engines": {
		"node": "13.0.0"
	}
}